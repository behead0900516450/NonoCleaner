# v0.1.0-beta Artifact QA

Status: **Local QA candidate — not approved for public release**

The candidate was prepared on 2026-09-15. It must not be uploaded to GitHub Releases until Developer ID signing, Apple notarization, Gatekeeper assessment, and isolated-account or clean-machine validation all pass.

## Application Metadata

| Field | Result |
| --- | --- |
| Release name | `v0.1.0-beta` |
| `CFBundleShortVersionString` | `0.1.0` |
| `CFBundleVersion` | `1` |
| Bundle identifier | `com.nono.cleaner` |
| Minimum macOS | `13.0` |
| Architecture | `arm64` (thin Mach-O) |
| SDK used | macOS 15.4 SDK |

## Artifact Manifest

| Field | Result |
| --- | --- |
| Filename | `Nono-Cleaner-0.1.0-beta.dmg` |
| Size | 694,378 bytes |
| SHA-256 | `b3913c9d1007b92390f33fdc021be0f968ac26847f5be3dcd466c1e86cc98e2d` |
| App signing identity | Ad-hoc (`TeamIdentifier` not set) |
| Notarization | Not submitted — Developer ID prerequisite unavailable |
| Stapling | Not performed |
| Gatekeeper | Failed assessment; expected for an ad-hoc, unnotarized candidate |

The DMG checksum is valid. Its root contains only `Nono Cleaner.app` and an `Applications` shortcut.

## Build and Static QA

- Clean Release build completed.
- All 32 existing regression tests passed: 15 safety classification, 8 sorting, 4 origin knowledge, and 5 productization tests.
- `codesign --verify --deep --strict --verbose=2` passed for the ad-hoc signature.
- The app executable is arm64 and declares macOS 13.0 as its minimum OS.
- No entitlements, including Debug entitlements, are embedded.
- The packaged app contains only `Info.plist`, the executable, and the code-signature metadata.
- No tests, fixtures, scan-report files, logs, crash reports, or developer-only resources are packaged.
- Source paths are stripped from the packaged executable before signing.
- Repository and packaged-binary scans found no personal checkout path, email, common token pattern, private-key marker, or developer Team ID.

## Runtime QA

The app was launched directly from the mounted DMG on the build Mac.

- App launch: Pass
- First scan: Pass (5,681 items in the observed run)
- Low Risk, Review, and Protected results: Pass
- Running-app guard: Observed in the scan report
- Recent-modification guard: Observed in the scan report
- macOS protected-path handling: Pass; 44 protected paths were reported as safely skipped
- In-app scan report and mode controls: Pass
- Move-to-Trash confirmation: Not completed; no deletion was attempted

This is not a clean-environment result. Creating a new macOS user requires an administrator-approved account name and credential and was not automated. Installation into `/Applications`, first-run Gatekeeper behavior, and the Trash confirmation must be repeated in an isolated macOS account or on a second clean Apple Silicon Mac.

## Signing and Notarization Readiness

- `codesign`: Available
- `xcrun notarytool`: Available (1.1.2)
- `xcrun stapler`: Available
- Developer ID Application identity: Not available (`0 valid identities found`)
- Notary credentials: Not used or written to the repository

Use a Keychain profile or CI secrets for notarization credentials. Never place an Apple ID password, app-specific password, Team ID, API key, certificate, or private key in source control.

## Branding and Screenshots

- Production App Icon: Missing; requirements and empty asset structure are documented under `docs/branding/` and `Resources/AppIcon.iconset/`.
- Screenshot workflow: Ready.
- Sanitized screenshots: Not captured.
- Production demo-data mode: Intentionally not added.

## Release Decision

**Do not publish this DMG.** The candidate proves that packaging and runtime scanning work, but the public release remains blocked by:

1. Final App Icon production and visual QA.
2. Developer ID Application certificate installation.
3. Hardened Runtime and entitlement review for the final signed build.
4. Successful Apple notarization and ticket stapling.
5. Successful `spctl --assess` after notarization.
6. Isolated-account or clean-machine installation and Trash-confirmation QA.
7. Seven sanitized, privacy-reviewed screenshots.
